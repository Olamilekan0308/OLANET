import { createContext, useContext, useEffect, useMemo, useState, type ReactNode } from 'react';
export type AuthUser = { id: string; email?: string | null; user_metadata?: Record<string, unknown> | null; profile?: { id?: string; full_name?: string | null; username?: string | null; avatar_url?: string | null; bio?: string | null; department?: string | null; course?: string | null; } | null; };
export type ProfileUpdateInput = { username?: string | null; full_name?: string | null; bio?: string | null; avatar_url?: string | null; department?: string | null; course?: string | null; };
type ProfileResponse = { profile?: AuthUser['profile']; error?: string };
type AuthResponse = { user?: AuthUser; error?: string; needsEmailConfirmation?: boolean; verified?: boolean; profileCreated?: boolean; profileError?: { status: number; code?: string; message: string; details?: string; hint?: string } | null; };
type AuthContextValue = { status: 'loading'|'authenticated'|'unauthenticated'; user: AuthUser|null; login:(email:string,password:string)=>Promise<AuthResponse>; signup:(fullName:string,email:string,password:string)=>Promise<AuthResponse>; logout:()=>Promise<void>; updateProfile:(updates:ProfileUpdateInput)=>Promise<NonNullable<AuthUser['profile']>>; uploadAvatar:(file:File)=>Promise<NonNullable<AuthUser['profile']>> };
const AuthContext=createContext<AuthContextValue|null>(null);
const authCooldowns=new Map<string,number>();
function enforceCooldown(key:string,seconds:number){const now=Date.now();const until=authCooldowns.get(key)??0;if(until>now)throw new Error(`Please wait ${Math.ceil((until-now)/1000)} seconds before trying again.`);authCooldowns.set(key,now+seconds*1000);}
async function request(path:string,init?:RequestInit){const response=await fetch(`/api/auth/${path}`,{...init,cache:'no-store',credentials:'include',headers:{'Cache-Control':'no-store','Content-Type':'application/json',...init?.headers}});const data=(await response.json().catch(()=>({}))) as AuthResponse;if(!response.ok){if(response.status===429||data.error?.toLowerCase().includes('over_email_send_rate_limit'))throw new Error('Email sending is temporarily rate-limited. Please wait a few minutes before trying again.');throw new Error(data.error??'Something went wrong. Please try again.');}return data;}
async function profileRequest(path:string,init?:RequestInit){const isBinary=init?.body instanceof Blob||init?.body instanceof ArrayBuffer;const response=await fetch(`/api/profile/${path}`,{...init,cache:'no-store',credentials:'include',headers:{'Cache-Control':'no-store',...(isBinary?{}:{'Content-Type':'application/json'}),...init?.headers}});const data=(await response.json().catch(()=>({}))) as ProfileResponse;if(!response.ok)throw new Error(data.error??'Unable to update your profile.');if(!data.profile)throw new Error('Supabase returned no profile after saving.');return data.profile;}
function openPeopleForAuthenticatedUser(){if(typeof window==='undefined')return;const path=window.location.pathname;const intentionalHomeReturn=window.location.search.includes('from=people');if((path==='/'&&!intentionalHomeReturn)||path==='/login'||path==='/signup'){window.location.replace('/people');}}
function LoadingScreen(){return <div style={{minHeight:'100vh',display:'grid',placeItems:'center',background:'#f0f2f5',fontFamily:'Inter,system-ui,sans-serif'}}><div style={{textAlign:'center'}}><div style={{margin:'0 auto 12px',width:52,height:52,borderRadius:16,display:'grid',placeItems:'center',background:'#1877f2',color:'#fff',fontWeight:900,fontSize:24}}>O</div><div style={{fontWeight:800,color:'#1c1e21'}}>OLANET</div><div style={{marginTop:5,fontSize:12,color:'#65676b'}}>Loading your feed…</div></div></div>}
export function AuthProvider({children}:{children:ReactNode}){const [status,setStatus]=useState<AuthContextValue['status']>('loading');const [user,setUser]=useState<AuthUser|null>(null);useEffect(()=>{let active=true;request('session').then(data=>{if(!active)return;setUser(data.user??null);setStatus(data.user?'authenticated':'unauthenticated');if(data.user)openPeopleForAuthenticatedUser()}).catch(()=>{if(!active)return;setUser(null);setStatus('unauthenticated')});return()=>{active=false}},[]);const value=useMemo<AuthContextValue>(()=>({status,user,async login(email,password){const data=await request('login',{method:'POST',body:JSON.stringify({email,password})});setUser(data.user??null);setStatus('authenticated');if(data.user)openPeopleForAuthenticatedUser();return data;},async signup(fullName,email,password){const normalized=email.trim().toLowerCase();enforceCooldown(`signup:${normalized}`,15);const data=await request('signup',{method:'POST',body:JSON.stringify({fullName,email:normalized,password})});if(data.user&&!data.profileError){setUser(data.user);setStatus('authenticated');openPeopleForAuthenticatedUser()}return data;},async logout(){await request('logout',{method:'POST'}).catch(()=>undefined);setUser(null);setStatus('unauthenticated');},async updateProfile(updates){const profile=await profileRequest('me',{method:'PUT',body:JSON.stringify(updates)});setUser(current=>current?{...current,profile}:current);return profile;},async uploadAvatar(file){const profile=await profileRequest('avatar',{method:'POST',headers:{'Content-Type':file.type},body:file});setUser(current=>current?{...current,profile}:current);return profile;}}),[status,user]);if(status==='loading')return <AuthContext.Provider value={value}><LoadingScreen/></AuthContext.Provider>;return <AuthContext.Provider value={value}>{children}</AuthContext.Provider>}
export function useAuth(){const context=useContext(AuthContext);if(!context)throw new Error('useAuth must be used within AuthProvider');return context;}
