(() => {
  const toast = (message) => {
    let el = document.getElementById('olanet-action-toast');
    if (!el) { el = document.createElement('div'); el.id = 'olanet-action-toast'; Object.assign(el.style,{position:'fixed',left:'50%',bottom:'80px',transform:'translateX(-50%)',zIndex:'100000',background:'#1d4348',color:'#fff',padding:'10px 14px',borderRadius:'12px',font:'700 13px system-ui,sans-serif',boxShadow:'0 10px 30px rgba(0,0,0,.2)',opacity:'0',transition:'opacity .18s'}); document.body.appendChild(el); }
    el.textContent=message; el.style.opacity='1'; clearTimeout(el._timer); el._timer=setTimeout(()=>{el.style.opacity='0'},2200);
  };
  const key=(prefix,id)=>`olanet:${prefix}:${id}`; const saved=(prefix,id)=>localStorage.getItem(key(prefix,id))==='1'; const toggleSaved=(prefix,id)=>{const next=!saved(prefix,id);localStorage.setItem(key(prefix,id),next?'1':'0');return next;}; const closestTest=(target,prefix)=>target.closest(`[data-testid^="${prefix}"]`);
  const esc=(value)=>String(value??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));

  const mountMobileDock=()=>{
    document.querySelectorAll('nav.fixed.bottom-0').forEach((n)=>{n.style.display='none'});
    if(document.getElementById('olanet-mobile-dock'))return;
    const chatLauncher=document.querySelector('button[aria-label="Open OLANET Social"]'); if(!chatLauncher)return; chatLauncher.style.display='none';
    const dock=document.createElement('nav'); dock.id='olanet-mobile-dock'; dock.setAttribute('aria-label','OLANET navigation');
    dock.innerHTML=`<style>@media(min-width:768px){#olanet-mobile-dock{display:none!important}}#olanet-mobile-dock{position:fixed;left:8px;right:8px;bottom:8px;z-index:99998;height:64px;border:1px solid var(--olanet-border,#e2e2e2);border-radius:18px;background:var(--olanet-surface,#fff);box-shadow:0 8px 30px rgba(0,0,0,.12);display:grid;grid-template-columns:repeat(5,1fr);padding:5px;font-family:system-ui,sans-serif}#olanet-mobile-dock a,#olanet-mobile-dock button{border:0;background:transparent;color:var(--olanet-text,#111);display:flex;flex-direction:column;align-items:center;justify-content:center;gap:2px;border-radius:13px;font:700 10px system-ui,sans-serif;cursor:pointer;text-decoration:none}#olanet-mobile-dock a.active,#olanet-mobile-dock button.active{background:var(--olanet-hover,#f0f2f5);color:#1877f2}#olanet-mobile-dock svg{width:22px;height:22px;fill:none;stroke:currentColor;stroke-width:2}</style><a href="/" data-dock="home" aria-label="Home"><svg viewBox="0 0 24 24"><path d="m3 10 9-7 9 7v10a1 1 0 0 1-1 1h-5v-6H9v6H4a1 1 0 0 1-1-1z"/></svg><span>Home</span></a><button type="button" data-dock="chat" aria-label="Chat"><svg viewBox="0 0 24 24"><path d="M20 11.5a7.5 7.5 0 0 1-7.5 7.5H8l-4 3 1.2-4.1A7.5 7.5 0 1 1 20 11.5Z"/></svg><span>Chat</span></button><a href="/circles" data-dock="departments" aria-label="Departments"><svg viewBox="0 0 24 24"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg><span>Departments</span></a><a href="/jobs" data-dock="jobs" aria-label="Opportunities"><svg viewBox="0 0 24 24"><rect x="3" y="7" width="18" height="13" rx="2"/><path d="M8 7V5a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2M3 12h18"/></svg><span>Jobs</span></a><a href="/profile" data-dock="menu" aria-label="Menu"><svg viewBox="0 0 24 24"><circle cx="12" cy="8" r="3"/><path d="M5 21a7 7 0 0 1 14 0M4 4h.01M20 4h.01"/></svg><span>Menu</span></a>`;
    document.body.appendChild(dock); dock.querySelector('[data-dock="chat"]').addEventListener('click',()=>chatLauncher.click());
    const path=location.pathname; dock.querySelectorAll('[data-dock]').forEach((el)=>{const k=el.getAttribute('data-dock');if((k==='home'&&path==='/')||(k==='departments'&&path.startsWith('/circles'))||(k==='jobs'&&path.startsWith('/jobs'))||(k==='menu'&&path.startsWith('/profile')))el.classList.add('active')});
  };
  new MutationObserver(mountMobileDock).observe(document.documentElement,{childList:true,subtree:true}); mountMobileDock();

  const mountPeopleSearch=()=>{
    const input=document.querySelector('[data-testid="input-global-search"]');
    if(!input||input.dataset.olanetSearchBound==='1')return;
    input.dataset.olanetSearchBound='1';
    let timer=0;
    let box=document.getElementById('olanet-global-results');
    if(!box){ box=document.createElement('div'); box.id='olanet-global-results'; Object.assign(box.style,{position:'fixed',zIndex:'99999',width:'min(390px,calc(100vw - 24px))',maxHeight:'420px',overflow:'auto',background:'#fffaf1',border:'1px solid #dfd2c0',borderRadius:'16px',boxShadow:'0 14px 40px rgba(29,67,72,.16)',padding:'6px',display:'none'}); document.body.appendChild(box); }
    const position=()=>{const r=input.getBoundingClientRect();box.style.top=`${Math.min(window.innerHeight-430,r.bottom+8)}px`;box.style.left=`${Math.max(8,Math.min(window.innerWidth-r.width-8,r.left))}px`;box.style.width=`${Math.max(280,Math.min(390,r.width+150))}px`;};
    const render=(people)=>{ if(!people.length){box.innerHTML='<div style="padding:16px;text-align:center;color:#789093;font:600 13px system-ui">No OLANET members found.</div>'; } else { box.innerHTML=people.map(p=>`<div data-person-id="${esc(p.id)}" style="display:flex;align-items:center;gap:10px;padding:10px;border-radius:12px"><div style="width:38px;height:38px;border-radius:50%;overflow:hidden;background:#e8c17a;display:grid;place-items:center;font:800 12px system-ui;color:#1d4348">${p.avatar_url?`<img src="${esc(p.avatar_url)}" alt="" style="width:100%;height:100%;object-fit:cover">`:esc((p.full_name||p.username||'OM').split(/\\s+/).map(x=>x[0]).join('').slice(0,2).toUpperCase())}</div><div style="min-width:0;flex:1"><div style="font:800 13px system-ui;color:#1d4348;white-space:nowrap;overflow:hidden;text-overflow:ellipsis">${esc(p.full_name||p.username||'OLANET member')}</div><div style="font:500 11px system-ui;color:#789093">${esc(p.username?`@${p.username}`:'OLANET member')}</div></div><button data-person-action="friend" data-person="${esc(p.id)}" style="border:0;border-radius:9px;padding:7px 9px;background:#dceceb;color:#2f817d;font:800 11px system-ui;cursor:pointer">Add friend</button><button data-person-action="message" data-person="${esc(p.id)}" style="border:0;border-radius:9px;padding:7px 9px;background:#1d4348;color:#fffaf1;font:800 11px system-ui;cursor:pointer">Message</button></div>`).join(''); } box.style.display='block'; position(); };
    input.addEventListener('input',()=>{clearTimeout(timer);const q=input.value.trim();if(!q){box.style.display='none';return;}timer=window.setTimeout(async()=>{try{const r=await fetch('/api/people?q='+encodeURIComponent(q),{credentials:'include'});const data=await r.json().catch(()=>[]);if(!r.ok)throw new Error(data?.error||'Search failed');render(Array.isArray(data)?data:[]);}catch(e){box.innerHTML='<div style="padding:14px;color:#a24f42;font:700 12px system-ui">Unable to search members.</div>';box.style.display='block';position();}},180);});
    input.addEventListener('focus',()=>{if(input.value.trim())position();});
    window.addEventListener('resize',()=>{if(box.style.display!=='none')position();});
  };
  new MutationObserver(mountPeopleSearch).observe(document.documentElement,{childList:true,subtree:true}); mountPeopleSearch();

  document.addEventListener('click',async(event)=>{
    const target=event.target instanceof Element?event.target:null;if(!target)return;
    const personAction=target.closest('[data-person-action]');
    if(personAction){
      const userId=personAction.getAttribute('data-person'); const action=personAction.getAttribute('data-person-action'); if(!userId)return;
      personAction.setAttribute('disabled','true');
      try{
        if(action==='friend'){
          const r=await fetch('/api/friends/'+encodeURIComponent(userId),{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'}});const data=await r.json().catch(()=>null);if(!r.ok)throw new Error(data?.error||'Could not send friend request');personAction.textContent='Requested';personAction.style.opacity='.65';toast('Friend request sent');
        } else {
          const r=await fetch('/api/messages/conversations',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({user_ids:[userId]})});const data=await r.json().catch(()=>null);if(!r.ok)throw new Error(data?.error||'Could not start conversation');window.location.href='/messages';
        }
      }catch(e){personAction.removeAttribute('disabled');toast(e instanceof Error?e.message:'Action failed');} return;
    }
    const searchBox=document.getElementById('olanet-global-results');const searchInput=document.querySelector('[data-testid="input-global-search"]');if(searchBox&&searchInput&&!searchBox.contains(target)&&target!==searchInput){searchBox.style.display='none';}
    const play=closestTest(target,'button-play-video-'); if(play){const card=play.closest('[data-testid^="card-video-"]');const title=card?.querySelector('h3')?.textContent||'Video';const overlay=document.createElement('div');overlay.setAttribute('data-olanet-video-overlay','1');Object.assign(overlay.style,{position:'fixed',inset:'0',zIndex:'99999',background:'rgba(17,40,43,.82)',display:'grid',placeItems:'center',padding:'20px'});overlay.innerHTML=`<div style="width:min(680px,100%);background:var(--olanet-surface,#fff);color:var(--olanet-text,#111);border-radius:22px;padding:22px"><div style="display:flex;justify-content:space-between;gap:12px;align-items:center"><strong style="font:800 20px system-ui,sans-serif;color:var(--olanet-text,#111)">${esc(title)}</strong><button data-olanet-video-close style="border:0;background:var(--olanet-hover,#f1f1f1);color:var(--olanet-text,#111);border-radius:50%;font-size:24px;cursor:pointer">×</button></div><div style="margin-top:16px;aspect-ratio:16/9;border-radius:16px;background:var(--olanet-surface-2,#f0f2f5);display:grid;place-items:center;color:var(--olanet-muted,#555);font:800 15px system-ui,sans-serif">Video player ready</div><p style="margin:14px 0 0;color:var(--olanet-muted,#666);font:600 13px system-ui,sans-serif">Connect the final video source to play uploaded media here.</p></div>`;document.body.appendChild(overlay);overlay.querySelector('[data-olanet-video-close]').addEventListener('click',()=>overlay.remove());overlay.addEventListener('click',(e)=>{if(e.target===overlay)overlay.remove()});return;}
    const share=closestTest(target,'button-share-post-');if(share){const card=share.closest('[data-testid^="card-post-"]');const match=card?.getAttribute('data-testid')?.match(/card-post-(.+)$/);const postId=match?.[1];const text=card?.querySelector('p.text-\\[15px\\]')?.textContent||card?.querySelector('p')?.textContent||'OLANET post';try{if(postId){const r=await fetch('/api/posts/'+encodeURIComponent(postId)+'/share',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'}});const data=await r.json().catch(()=>null);if(!r.ok)throw new Error(data?.error||'Unable to record share');}if(navigator.share)await navigator.share({title:'OLANET',text});else{await navigator.clipboard.writeText(text);toast('Post shared and text copied');}}catch(e){toast(e instanceof Error?e.message:'Share cancelled')}return;}
    const sendComment=closestTest(target,'button-send-comment-');if(sendComment){const match=sendComment.getAttribute('data-testid')?.match(/button-send-comment-(.+)$/);const postId=match?.[1];const input=postId?document.querySelector(`[data-testid="input-comment-${CSS.escape(postId)}"]`):null;const body=input?.value?.trim();if(postId&&body){try{const r=await fetch('/api/posts/'+encodeURIComponent(postId)+'/comments',{method:'POST',credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({body})});if(!r.ok){const data=await r.json().catch(()=>null);throw new Error(data?.error||'Unable to save comment');}}catch(e){toast(e instanceof Error?e.message:'Unable to save comment');}}return;}
    const menu=closestTest(target,'button-post-menu-');if(menu){document.querySelectorAll('[data-olanet-post-menu]').forEach(x=>x.remove());const box=document.createElement('div');box.setAttribute('data-olanet-post-menu','1');Object.assign(box.style,{position:'absolute',zIndex:'1000',background:'var(--olanet-surface,#fff)',color:'var(--olanet-text,#333)',border:'1px solid var(--olanet-border,#ddd)',borderRadius:'12px',padding:'6px',boxShadow:'0 10px 30px rgba(0,0,0,.12)'});box.innerHTML='<button data-report-post style="display:block;width:100%;border:0;background:transparent;padding:9px 12px;text-align:left;font:700 12px system-ui,sans-serif;color:var(--olanet-text,#333);cursor:pointer">Report / hide</button>';const rect=menu.getBoundingClientRect();box.style.left=`${Math.max(8,rect.right-150)}px`;box.style.top=`${rect.bottom+6}px`;document.body.appendChild(box);box.querySelector('[data-report-post]').addEventListener('click',()=>{box.remove();toast('Thanks. Your report was noted.')});return;}
    const saveJob=closestTest(target,'button-save-job-');if(saveJob){const id=saveJob.dataset.testid;const on=toggleSaved('job',id);saveJob.setAttribute('aria-pressed',String(on));saveJob.style.color=on?'#1877f2':'';toast(on?'Opportunity saved':'Opportunity removed from saved');return;}
    const saveCalc=closestTest(target,'button-save-calculation');if(saveCalc){toast('Calculation saved for this session');return;}
    const viewJob=closestTest(target,'button');if(viewJob&&viewJob.textContent?.trim()==='View opportunity'){toast('Opening opportunity details');return;}
  },true);
})();
